NODE_ENV=development
HOST=127.0.0.1
PORT=3000
LOG_LEVEL=info

# Optional for /health; required for workspace persistence and migrations.
# Supply DATABASE_URL securely in the process environment (postgresql://...).
