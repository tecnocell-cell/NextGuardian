# domain

Modelos Account, User, Device, ActivationCode, Subscription e estados separados; transições puras em src/main/kotlin. Nenhuma dependência Android.

As fontes desta pasta são compiladas pelo módulo :app; não há módulo Gradle separado.
